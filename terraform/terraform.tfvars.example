# Example terraform.tfvars file
# Copy this to terraform.tfvars and update with your actual values

# AWS Configuration
aws_region = "us-east-1"

# DynamoDB Configuration
dynamodb_table_name = "reddit-ingest-state"

# S3 Configuration - MUST be globally unique
# Replace 'your-unique-identifier' with something unique to you
s3_bucket_name = "aws-image-translate-your-unique-identifier"

# Project Configuration
project_name = "aws-image-translate"
environment  = "dev"  # Options: dev, staging, prod

# Note: Reddit API credentials are now managed separately in .env.local
# This keeps infrastructure configuration separate from application secrets
