# .env.example
# This file is a template for environment variables.
# Copy this file to .env.local and fill in the values.
#    cp .env.example .env.local

# ⚠️  IMPORTANT: Only AWS CLI credentials are required for deployment!
#    Configure AWS CLI with: aws configure
#    All credentials below are OPTIONAL - the app works without them.

# --- Optional: Reddit Integration ---

# Reddit API Configuration (for populating the gallery with images from subreddits)
# Leave blank to disable Reddit features - app will still work with MMID dataset
# Get these from https://www.reddit.com/prefs/apps/
REDDIT_CLIENT_ID=your_client_id_here
REDDIT_CLIENT_SECRET=your_client_secret_here
REDDIT_USER_AGENT="python:translate-images-bot:1.0 (by u/YourUsernameHere)"

# Reddit Subreddits Configuration
# List of subreddits to monitor for images with translatable text
# Add subreddits as a comma-separated list (no spaces around commas)
# Examples: translator,language,learn,French,Spanish
REDDIT_SUBREDDITS=translator,language


# --- Optional: Google OAuth Integration ---

# Google OAuth Configuration (enables "Sign in with Google" button)
# Leave blank to disable Google OAuth - users can still sign in with Cognito
# Get these from https://console.cloud.google.com/
GOOGLE_OAUTH_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your_google_client_secret


# --- Optional: CI/CD Pipeline Integration ---

# GitHub CodeStar Connection ARN (for automated CI/CD pipeline)
# Leave blank to disable CI/CD pipeline - app will still deploy manually
# Get this from AWS Console > Developer Tools > Settings > Connections
GITHUB_CONNECTION_ARN=arn:aws:codestar-connections:us-east-1:YOUR_ACCOUNT_ID:connection/YOUR_CONNECTION_ID
